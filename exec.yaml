name: first workflow 

on: workflow_dispartch
job:
  firstjob:
    runs-on: ubuntu-latest
    steps:
      - name: Print greeting
        run: echo "Hello World"
      - name: Print goodbye
        runs: echo "Done - bye!"